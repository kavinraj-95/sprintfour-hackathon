fastapi>=0.118
uvicorn[standard]>=0.34
pydantic>=2.12
pytest>=8.3
httpx>=0.28
anthropic>=0.69

# NER layer (layer 3). Presidio + a small spaCy model — chosen over GLiNER to
# avoid a torch (~0.5 GB) dependency; see fixtures/ner-benchmark.md. After
# installing, download the model once:  python -m spacy download en_core_web_sm
presidio-analyzer>=2.2
spacy>=3.8

# Raster (scanned-document) path: OCR + pixel redaction. pytesseract needs the
# Tesseract binary installed at the OS level (e.g. `pacman -S tesseract` /
# `apt install tesseract-ocr`). PyMuPDF rasterises PDF pages; Pillow does the
# pixel overwrite + re-encode.
pytesseract>=0.3
pymupdf>=1.24
Pillow>=10
